import { Routes } from '@angular/router';
import { CustomersPageComponent } from './pages/customers-page.component';

export const CUSTOMERS_ROUTES: Routes = [{ path: '', component: CustomersPageComponent }];
