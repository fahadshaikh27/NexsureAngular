import { Routes } from '@angular/router';
import { RaiseTicketPageComponent } from './pages/raise-ticket-page.component';

export const RAISE_TICKET_ROUTES: Routes = [{ path: '', component: RaiseTicketPageComponent }];
