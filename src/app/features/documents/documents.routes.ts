import { Routes } from '@angular/router';
import { DocumentsPageComponent } from './pages/documents-page.component';

export const DOCUMENTS_ROUTES: Routes = [{ path: '', component: DocumentsPageComponent }];
