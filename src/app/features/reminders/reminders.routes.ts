import { Routes } from '@angular/router';
import { RemindersPageComponent } from './pages/reminders-page.component';

export const REMINDERS_ROUTES: Routes = [{ path: '', component: RemindersPageComponent }];
