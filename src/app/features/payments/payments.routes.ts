import { Routes } from '@angular/router';
import { PaymentsPageComponent } from './pages/payments-page.component';

export const PAYMENTS_ROUTES: Routes = [{ path: '', component: PaymentsPageComponent }];
